import {NextRequest,NextResponse} from 'next/server';import {withAuth,finance,ApiError} from '@/lib/server';import {z} from 'zod';
export async function GET(req:NextRequest,{params}:{params:Promise<{id:string}>}){return withAuth(req,async(db,_user,profile)=>{finance(profile);const id=z.string().uuid().parse((await params).id);const {data:doc,error}=await db.from('documents').select('storage_path,document_number').eq('id',id).eq('state','ready').single();if(error||!doc)throw new ApiError(404,'Document not found.');const {data,error:storageError}=await db.storage.from('financial-documents').createSignedUrl(doc.storage_path,60,{download:doc.document_number+'.pdf'});if(storageError||!data?.signedUrl)throw new ApiError(502,'Unable to retrieve the document. Please try again.');return NextResponse.redirect(data.signedUrl,303);});}
